<section data-section-id="{{section.id}}" data-type="logo-list"{% if section.settings.section_width == 'full-width' %} class="full-width"{% endif %}>
  {% if section.settings.title != blank %}
  <h3 class="general-title" data-theme-editor-setting="title" >{{section.settings.title}}</h3>
  {% endif %}

  {% if section.blocks.size > 0 %}
  <div class="grid">
    <ul>
      {% for block in section.blocks %}
      <li data-section-block-id="{{block.id}}">
        {% if block.settings.link != blank %}
        <a href="{{block.settings.link}}" target="_blank">
        {% endif %}

        {% if block.settings.image != blank %}
          <img src="{{block.settings.image}}">
        {% endif %}

        {% if block.settings.link != blank %}
        </a>
        {% endif %}
      </li>
      {% endfor %}
    </ul>
  </div>
  {% endif %}

</section>
